import React from 'react';
import Nav from '../Nav/Nav';
import './Header.scss';

function Header () {
    return (
      <header className="header">
        <Nav />
      </header>
    );
}

export default Header;
